# illusion
illusion是一个go语言web框架，目标是实现一个简洁好用的web框架。目前illusion功能包括自定义消息处理器、自定义过滤器、自定义request转换器、自定义response转换器、
自定义参数类型转换器、自定义全局异常处理器、自定义监听器、自定义日志。
## 消息处理器
消息处理器是用于处理业务逻辑的，自定义消息处理器需要先定义一个struct，然后再struct上定义个一个成员函数，
成员函数必须是public方法，也就是大写字母开头的方法。只能有一个struct指针类型的入参，出参必须有两个，第一个是一个struct指针类型，第二个是error

